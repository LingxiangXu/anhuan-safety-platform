# 需求入口 · 安全管理平台

> **本文件是「安全管理平台」所有需求输入的唯一入口。**
> 业务人员提交的需求、本人（徐翎翔）提出的需求，均在此登记；AI 助手会读取此处需求进行**二次优化与落地**，并把状态同步回本文件。
> 约定：不要把需求零散记在聊天里，统一进此文件，保证可追溯、可复用。

---

## 使用说明

- **新增需求**：复制文末「模板」段落，生成一个 `## REQ-YYYYMMDD-序号` 小节，填写字段即可。
- **来源（必填）**：`业务人员`（他人提交）/ `徐翎翔`（本人直接提出）。
- **状态流转**：`待评估` → `优化中`（AI 二次优化中）→ `已排期` → `开发中` → `已完成` / `已驳回`。
- **AI 二次优化**：AI 在「AI 二次优化」区补充 **子任务拆解 / 验收标准 / 技术建议 / 风险提示**，并可将状态推进到 `已排期`。
- **索引表**：每次新增或状态变更，AI 同步更新上方「索引」表，便于一眼浏览。
- **关联模块**：填写相关页面/组件（如 驾驶舱、RealMap、隐患督办），方便定位代码。

---

## 索引（快速浏览）

| ID | 标题 | 来源 | 优先级 | 状态 | 录入日期 |
|----|------|------|--------|------|----------|
| REQ-20260723-001 | （示例）地图支持放大与手动缩放 | 徐翎翔 | P1 | 已完成 | 2026-07-22 |
| REQ-20260723-002 | 关键动作前人员定位校验（巡检提交/监护确认） | 徐翎翔 | P0 | 优化中 | 2026-07-23 |
| REQ-20260724-001 | 特殊作业分级 + 分级作业票模板 | 业务人员 | P1 | 开发中 | 2026-07-24 |
| REQ-20260724-002 | 隐患来源梳理：巡检任务如何转隐患 / 闭环复查 | 业务人员 | P1 | 待评估 | 2026-07-24 |
| REQ-20260724-003 | 各业务场景审批节点梳理 | 业务人员 | P2 | 开发中 | 2026-07-24 |
| REQ-20260726-001 | 操作简化与流程瘦身：让一线用得简单 | 徐翎翔 | P0 | 优化中 | 2026-07-26 |
| REQ-20260726-002 | 按制度文件对齐审批流与作业票/隐患表单 | 徐翎翔 | P0 | 已完成 | 2026-07-26 |
| REQ-20260727-001 | 作业票详情按PDF《危险作业审批表》还原固定签字栏 | 徐翎翔 | P1 | 已完成 | 2026-07-27 |

---

## 需求明细

### REQ-20260723-001 （示例）地图支持放大与手动缩放
- **来源**：徐翎翔
- **提交人**：徐翎翔
- **录入日期**：2026-07-22
- **优先级**：P1
- **关联模块**：驾驶舱 / 风险四色图（RealMap.vue、Dashboard.vue、RiskManagement.vue）
- **状态**：已完成
- **原始需求**：
  - 地图上元素看着太小，希望默认能放大；现场演示时也能手动缩放。
- **AI 二次优化**：
  - **子任务拆解**：①RealMap 自适应改为只按"风险区域+风险点"缩放，排除庞大的厂区边界；②设最低缩放下限 = 传入 zoom；③加 +/- 手动缩放控件 + 滚轮缩放；④四色图地图高度 460→520px；⑤驾驶舱 zoom 16→19、关闭自适应。
  - **验收标准**：两处地图默认 zoom=19 居中；左上角有 +/- 且可滚轮缩放；无控制台报错（amap 离线 ERR 除外）。
  - **技术建议**：改 `RealMap.vue` `initMap/buildLayers` + `Dashboard.vue` `RealMap` 调用 + `RiskManagement.vue` 高度。
  - **风险提示**：高德底图需联网；离线仅地图区空白，其余界面正常。
- **处理记录**：
  - 2026-07-22 AI 二次优化并落地，生产构建 0 error，已提交 `d02b694`。

### REQ-20260723-002 关键动作前人员定位校验（巡检提交 / 监护确认）
- **来源**：徐翎翔（基于现场管理实际提出）
- **提交人**：徐翎翔
- **录入日期**：2026-07-23
- **优先级**：P0
- **关联模块**：巡检任务（InspectionManagement.vue + MobileField 嵌入执行）、特殊作业（SpecialWork.vue / WorkTicket.vue / MobileField 监护确认）、CheckIn.vue（围栏逻辑可复用）、safeData（personnel / workPermits / factoryZones）、新增 `utils/geoUtils.js`、新增 `LocationVerify.vue`
- **状态**：优化中（AI 二次优化完成，待排期落地）
- **原始需求**：
  - 在实际工作中，巡检任务"检查前"、特殊作业"监护人确认前"，都需要对相应人员进行**定位校验**，确认人确实到达现场，防止代操作/虚报。
- **AI 二次优化**：
  - **子任务拆解**：
    1. **数据层补齐**：`personnel` 加 `currentLocation{lng,lat,source,ts}`（演示用，默认在围栏内，可"模拟偏离"）；`factoryZones` 加 `radius`（巡检直接复用 zone 坐标+半径）；`workPermits` 加 `workLocation{lng,lat,radius,zoneName}`（作业票当前无位置字段，需新增）。
    2. **工具层 `src/utils/geoUtils.js`**：`haversine(a,b)` 算米数；`isInsideGeofence(personLoc, geofence, buffer)` 判定；`getCurrentLocation()` 取 GPS/模拟；`canLocate()` 判断定位可用性。把 CheckIn.vue 里散落的"厂区范围"判定统一迁过来。
    3. **巡检校验**：MobileField 巡检"提交结果"按钮前置 `verifyLocation(assigneeId, task.zoneGeofence)`；不通过在围栏内则禁用提交并提示"您距巡检区域 X 偏差 Y 米"。
    4. **监护校验**：`confirmGuardian()`（三处）前置 `verifyLocation(guardianId, permit.workLocation)`；不通过在场则禁用"确认并允许开工"。
    5. **容错降级**：定位不可用/无网 → 不允许静默通过，走"离线报备"（填原因+上级审批留痕）。
    6. **留痕审计**：校验结果写入对应 timeline `{time, action:'定位校验通过/未通过', operator, distance, geofence, deviceSource}`，可供领导/审计查"是否真到现场"。
    7. **演示开关**：加"模拟定位"控件（在围栏内 / 偏离 N 米），便于现场复现"通过/拦截"两种状态。
  - **验收标准**：
    - 操作人不在围栏内时，巡检提交 / 监护确认按钮禁用，并提示偏差米数。
    - 在围栏内时校验通过，正常进入下一步。
    - 定位不可用时明确提示而非静默放行，走报备流程并留痕。
    - 每次校验写入 timeline（时间/人/距离/结果/设备源）。
    - 演示开关可稳定复现"通过/拦截"。
  - **技术建议**：
    - 新增纯函数 `geoUtils.js`（易测）；统一 CheckIn 围栏判定迁移。
    - 围栏数据：巡检挂 `zone`（已有坐标）、作业挂 `permit.workLocation`（需新增）；人员位置用 `personnel.currentLocation`（演示）。
    - 校验 UI 抽成可复用 `LocationVerify.vue`（地图显示人点+围栏圈+距离+状态），两处共用，沿用 RealMap 画点能力。
    - 严格度分级：特殊作业（高危）= 硬阻断；巡检 = 可配硬阻断或"软警示+强制说明"。
  - **风险提示**：
    - **GPS 室内漂移大**：高危作业若仅靠手机 GPS 易误拦截，建议现场配 UWB/蓝牙信标/二维码扫码定位。
    - **隐私合规**：位置仅在校验瞬间取用、需授权告知，不做持续后台追踪。
    - **离线/无网**：不能阻断真实作业，需离线报备兜底。
    - **演示数据依赖**：personnel / workPermits 必须补位置字段，否则校验无依据（属本轮必做项）。
- **处理记录**：
  - 2026-07-23 用户提出；AI 完成二次优化（落地方案），登记为 P0、状态优化中。

### REQ-20260724-001 特殊作业分级 + 分级作业票模板
- **来源**：业务人员
- **提交人**：（待补）
- **录入日期**：2026-07-24
- **优先级**：P1
- **关联模块**：特殊作业（SpecialWork.vue / WorkTicket.vue / MobileField 监护确认）、safeData（`workPermits` / `workTypeConfig`）、新增 `workPermitTemplates` 模板库
- **状态**：待评估
- **原始需求**：
  - 特殊作业会分级，不同级别对应不同的作业票模板（措施清单、字段、审批链不同）。
- **AI 二次优化**（现状核对 + 落地建议）：
  - **现状核对**：`safeData.js` 第 864-866 行 `workTypeConfig` 已含分级描述（高处作业：一级/二级/三级/特级；吊装：常规/特殊；临时用电：—）和 `measuresCount`；但 `workPermits` 数据（352 行起）**尚无 `level` 字段、无 `templateId`/`template` 字段**；`sceneFlows.js` 特殊作业主流程第 44 行已提到"车间主任 / 分管副总分级审批"。即"分级意识"已有，但"分级→模板→审批"未建模。
  - **子任务拆解**：
    1. 定义作业分级标准枚举（参考 GB 30871：动火一级~特级、高处一级~特级、吊装常规/特殊等），与 `workTypeConfig` 对齐。
    2. 建 `workPermitTemplates` 模板库：按 `类型 + 级别` 索引，每级含「安全措施清单 / 必填字段 / 审批链」。
    3. `workPermits` 加 `level` + `templateId` 字段；新建作业票时按 类型+级别 自动套用模板。
    4. 审批节点随级别变化（如特级需分管副总，常规仅需车间主任）——与 REQ-20260724-003 审批节点联动。
    5. 演示：可切换级别实时看模板（措施/字段/审批链）变化。
  - **验收标准**：选类型+级别→自动带出对应模板；不同级别审批链不同；模板切换无报错；演示可复现。
  - **技术建议**：复用并扩展现有 `workTypeConfig`（加 level + template 引用）；新增 `workPermitTemplates` 数据源；WorkTicket/SpecialWork 新建页按 `level` 渲染差异化模板。
  - **风险提示**：分级与模板需与业务/安环确认权威口径（国标/公司制度）；模板差异要覆盖齐全（措施项、审批角色、签字栏）；避免把演示简化态当真实制度。

### REQ-20260724-002 隐患来源梳理：巡检任务如何转隐患 / 闭环复查
- **来源**：业务人员
- **提交人**：（待补）
- **录入日期**：2026-07-24
- **优先级**：P1
- **关联模块**：隐患与督办（HazardSupervision.vue）、巡检任务（InspectionManagement.vue + MobileField）、safeData（`inspectionTasks` / `hazards`）、随手拍（MobileField 第 771 行）
- **状态**：待评估
- **原始需求**：
  - 隐患的巡检任务从哪里来？（即巡检与隐患的生成链路、来源归属如何定义）
- **AI 二次优化**（现状核对 + 落地建议）：
  - **现状核对**：`inspectionTasks`（587 行）有日常/专项巡检（`zoneId`/`checkItems`/`checklist`）；`hazards`（292 行）有 `hazardId` 但**无来源外键**——未标注来自巡检/随手拍/举报；`MobileField` 第 771 行"随手拍上报"是隐患来源之一，且隐患→进入安环受理流程。即"巡检发现→转隐患""隐患整改→复查巡检"两条闭环链路**均未建模**。
  - **子任务拆解**：
    1. 隐患来源分类枚举（`sourceType`：巡检发现 / 随手拍 / 监督举报 / 系统监测 / 上级交办）；`hazards` 加 `sourceType` + `sourceRefId`（关联 `inspectionTaskId` / `checkItemId`）。
    2. 巡检项勾选"异常"→一键转隐患（预填区域/描述/责任人，状态=待核查），双向可追溯。
    3. 隐患整改完成→自动生成"复查巡检任务"（关联 `hazardId`，闭环）。
    4. 来源看板：按 `sourceType` 统计隐患构成（哪类来源占比高）。
  - **验收标准**：隐患详情可见来源链路（来自哪个巡检/哪项/谁上报）；巡检异常一键生成隐患且双向可追溯；整改后自动派复查任务；来源构成可统计。
  - **技术建议**：`hazards` 加 `sourceType`/`sourceRefId`；InspectionManagement/MobileField 巡检提交异常时调用"转隐患"；HazardSupervision 整改验收节点触发复查巡检生成。
  - **风险提示**：巡检结果与隐患字段映射要清晰；避免重复生成（幂等）；演示数据需补 `sourceType` 样例；"复查巡检"要与常规巡检区分防混淆。

### REQ-20260724-003 各业务场景审批节点梳理
- **来源**：业务人员
- **提交人**：（待补）
- **录入日期**：2026-07-24
- **优先级**：P2
- **关联模块**：风险（safeData `_approvalState`）、巡检、隐患与督办、特殊作业（WorkTicket / MobileField `approvalChain`）、OA 审批集成（sceneFlows 第 62 行）、培训中心（TrainingManagement）、消息中心（MessageCenter 超时预警）
- **状态**：待评估
- **原始需求**：
  - 各个业务场景的审批节点（链路、角色、时限、驳回）需要梳理清楚。
- **AI 二次优化**（现状核对 + 落地建议）：
  - **现状核对**（审批节点目前散落各处）：
    - 风险：`safeData` 风险点 `_approvalState`/`_approvalHistory`（提交 LEC 评价→审批）；`sceneFlows` 风险主流程"风险点审批通过正式生效并归档"。
    - 巡检：`sceneFlows` 巡检主流程（派发→执行→提交→审核）。
    - 隐患与督办：`sceneFlows` 隐患主流程（受理→核查→整改→验收→督办）。
    - 特殊作业：`WorkTicket` 状态机（草稿→待监护确认→待完工验收）；`MobileField` 第 1274 行 `approvalChain: '部门负责人 → 安环室 → 分管领导'`，状态含 待监护确认/待安环审核/待领导审批/待前置核验。
    - OA 审批集成：`sceneFlows` OA 主流程（需求分析→定义流程节点→联调测试）。
    - 培训：`TrainingManagement` 流程（制定计划→分配人员→学习签到→考试评分→形成档案）。
  - **子任务拆解**：
    1. 盘点 6+ 场景的审批/确认节点与角色，输出「审批节点矩阵」（场景 / 触发动作 / 节点链 / 审批角色 / 时限 / 驳回去向）。
    2. 统一审批节点数据模型（节点 = 角色 + 动作 + 时限 + 驳回到哪），新增 `approvalNodeConfig` 按场景/动作索引。
    3. 对未建模节点（隐患验收、培训归档等）补节点；统一各场景术语口径。
    4. 驾驶舱/消息中心接入"待我审批"与超时预警（已有超时预警样例 MessageCenter 第 63 行）。
  - **验收标准**：每个场景审批链路在系统内有清晰节点展示；角色/时限/驳回一致；消息中心能按节点推待办 + 超时预警。
  - **技术建议**：新增 `approvalNodeConfig` 数据源；复用现有 `_approvalState` 思路与 MobileField `approvalChain`；与 REQ-20260724-001（作业分级审批）共用节点模型。
  - **风险提示**：各场景审批实际口径需以业务/安环制度为准，**先梳理现状再对齐制度**，避免把演示简化态误当真实制度；矩阵需与现行管理文件逐一对齐。

### REQ-20260726-001 操作简化与流程瘦身：让一线用得简单
- **来源**：徐翎翔（产品体验方向，基于"实际使用的人操作简单、流程不复杂"诉求）
- **提交人**：徐翎翔
- **录入日期**：2026-07-26
- **优先级**：P0
- **关联模块**：全局（导航/菜单、角色门户）、MobileField（移动现场）、InspectionManagement、HazardSupervision、SpecialWork、Dashboard/消息中心、safeData
- **状态**：优化中（AI 完成二次优化方案，待排期落地）
- **原始需求**：
  - 平台实际使用者（一线巡检员、作业人员、监护人、隐患上报人、班组长、安全员）要操作简单、流程不复杂。当前系统偏"功能展示/汇报视角"，对一线不够友好，需要系统性体验瘦身。
- **AI 二次优化**（现状核对 + 落地方案）：
  - **现状痛点核对**（基于已读代码）：
    - 导航 13+ 项，含"平台架构/功能全景/角色与终端"等**汇报展示型页面**，对一线无意义，反而增加找功能成本。
    - 巡检、特殊作业、隐患分属不同模块，监护确认散在 3 处（SpecialWork/WorkTicket/MobileField）；一线要在多页面间跳转。
    - 录入多为手填：隐患、作业票、巡检结果靠打字，无拍照/语音/自动识别减负。
    - 审批链默认偏长（特殊作业 MobileField `approvalChain: 部门负责人→安环室→分管领导` 三级串行），低风险也被重审批。
    - 任务/待办分散，无"我的工作台"聚合；人找功能而非功能找人。
  - **子任务拆解**（六大方向，按优先级）：
    1. **【P0】角色化首页 / 我的工作台**：新增 `Workbench.vue`（或改造 Dashboard 入口），按角色聚合"今日任务 + 待我办 + 一键发起"；左菜单按角色裁剪（一线只留巡检/上报/作业，管理层留驾驶舱/督办）。把"平台架构/功能全景/角色与终端"折叠或移出一线菜单。
    2. **【P0】移动端任务驱动**：MobileField 改为"任务卡片流"——打开即今日任务（巡检/作业票/待确认隐患），点卡片进对应操作，不让人找功能；监护确认/巡检提交/隐患上报三件事在同一任务流内闭环（复用 REQ-20260723-002 定位校验）。
    3. **【P1】录入减负**：①隐患随手拍拍照 → AI/模板自动带出类型·等级·位置，人只确认（复用 HazardSupervision + MobileField 随手拍）；②巡检异常勾选一键转隐患草稿（与 REQ-20260724-002 联动）；③特殊作业选类型+级别自动带模板（与 REQ-20260724-001 联动）。
    4. **【P0】审批分级瘦身**：低风险/常规作业免审批或仅班组长确认；中高风险才走多级（与 REQ-20260724-001/003 联动）；串行改并行；移动端一键审批 + 电子签名；设作业票有效期/超时自动升级。
    5. **【P1】智能辅助与防呆**：风险四色图点区域直接联动该区待办/隐患，不让人记编号；统一状态机 + "申请在谁那、还要多久"进度可见；提交前汇总预览，失败给明确原因（如定位偏移 X 米）。
    6. **【P2】无障碍与容错**：离线/弱网可暂存本地事后同步；定位不可用走报备兜底（REQ-20260723-002）；大字/高对比模式适配现场环境。
  - **验收标准**：
    - 一线用户打开即见今日任务，3 步内完成核心操作（巡检提交/作业确认/隐患上报）。
    - 左菜单按角色只显示相关项，汇报型页面不占一线入口。
    - 低风险作业无需多余审批；审批可在移动端一键完成。
    - 录入环节打字量下降 ≥60%（拍照/勾选/自动带出替代）。
    - 离线可暂存、联网后同步；定位失败不阻断真实作业。
  - **技术建议**：
    - 新增 `Workbench.vue` + 角色→菜单映射配置 `roleMenuConfig`；现有 `Dashboard/Index/SideNav` 改造而非推倒。
    - 移动端以"任务"为中心重构 MobileField 交互，"功能"降为次级入口。
    - 审批/模板/来源三套配置（approvalNodeConfig / workPermitTemplates / sourceType）统一由 REQ-20260724 三条需求提供数据底座，本需求负责"前端收敛与流程编排"。
  - **风险提示**：
    - 角色裁剪别误伤管理视角的演示完整性（建议"演示模式"可切全量菜单）。
    - 审批瘦身须以安全管理制度为准，不能为了简单而取消法定必须审批（GB 30871 高危作业分级审批不可省）。
    - AI 自动识别隐患有误差，必须保留人确认环节，不能全自动。
- **处理记录**：
  - 2026-07-26 用户提出"让实际使用的人操作简单"；AI 完成二次优化（六大方向方案 + 验收/技术/风险），登记 P0、状态优化中。

### REQ-20260726-002 按制度文件对齐审批流与作业票/隐患表单
- **来源**：徐翎翔
- **提交人**：徐翎翔
- **录入日期**：2026-07-26
- **优先级**：P0
- **关联模块**：safeData.js（`workApprovalMatrix`/`hazardFlowConfig`/`workPermits`/`WORK_TYPE`/`workTypes`）、sceneFlows.js（specialwork/hazard）、SpecialWork.vue、WorkTicket.vue、HazardSupervision.vue、SceneFlow.vue
- **状态**：已完成
- **原始需求**：
  - 提供两份权威文件——`平台流程节点(1).xlsx`（隐患三套流程 + 危险作业分级审批链）与 `TYHIZDJ-B0523-2025A危险作业安全管控制度(1).pdf`（太原重工铸锻件分公司制度，含高处/临时用电/起重吊装/动火分级标准与各类审批表模板）。要求据此把系统"现有的流程审批"和"已有的表单样式"做对齐调整。
  - 边界确认：① 系统原仅高处/吊装/临时用电三种，需**一并补进动火作业**；② **完整对齐** PDF《危险作业审批表》表单样式。
- **AI 二次优化 / 落地记录**：
  - **审批链矩阵**：新增 `workApprovalMatrix`（按"作业类型+级别"逐级加签），依据 xlsx：高处一级3节点→二级4→三级5→特级6（到集团健康安全环保部）；临时用电走"设备能源室→健康安全管理室"、有效期15天；特殊起重吊装5节点（含设备能源室）；动火二级3节点/一级4/特级到集团武保部，有效期72h/8h/8h。提供 `getApprovalChain()`/`getWorkValidity()` 工具函数。
  - **修正既有 7 张作业票** `approvalChain`（原统一写死"部门负责人→安环室→分管领导"）为按类型+级别的真实链路，并补 `validity`/`workLevel`。
  - **新增动火类型**：`WORK_TYPE`/`workTypes` 增补 `FIRE`；新增 1 张二级动火示例票（含 7 项动火安全措施确认）。
  - **隐患三套流程**：新增 `hazardFlowConfig`（公司监督检查 9 节点 / 分公司计划检查 6 节点 / 隐患随手拍 6 节点）+ `hazardSourceToFlow` 来源映射。
  - **前端对齐**：SpecialWork 增动火 Tab、审批链节点化展示 + 有效期 + 分级标签、新建表单支持选级别并自动套审批链/有效期；WorkTicket 详情重构为《危险作业审批表》版式（作业部门/作业地点/作业证编号/作业类别/有效期/作业人签字栏 + 按作业类型差异化的安全措施确认清单，同时修复了原字段名不匹配 bug）；HazardSupervision 来源下拉对齐三套 + 详情按来源展示流转节点；SceneFlow 支持制度依据副标题。
  - **移动端（MobileField.vue）对齐**：作业票申请新增 🔥动火作业类型、LIFTING 文案改"起重吊装"；新建表单按类型显示级别下拉（高处作业一级~特级 / 动火作业一级~特级），切换类型自动重置级别；提交时按"类型+级别"调用 `getApprovalChain`/`getWorkValidity` 算出分级审批链与有效期（吊装强制"特殊"），写入作业票 `approvalChain`/`workLevel`/`validity`，并在表单内实时预览审批链、在提交结果展示级别与有效期；随手拍提交来源由"移动巡检"改为"隐患随手拍"，使其落入三套流程之一的 QUICK_SNAP。
  - **验收标准**：生产构建 0 error；各作业类型审批链与文件一致；动火类型可见可选；作业票/隐患表单字段与 PDF 审批表口径对齐。
- **处理记录**：
  - 2026-07-26 AI 解析两份文件 → 盘点现状偏差 → 落地 safeData/sceneFlows/3 个页面/SceneFlow 组件；生产构建通过。桌面演示包按用户偏好未主动刷新。
  - 2026-07-26（续）用户要求把前述改动对齐到"本项目移动端"，明确指向 v4 内部 `MobileField.vue`（非 anhuan-safety-platform 独立工程）。已落地移动端作业票申请（动火类型+级别下拉+审批链矩阵实时预览/计算）与随手拍来源对齐（QUICK_SNAP）；生产构建 0 error。
  - 关联推进：本次已实际落地 REQ-20260724-001（分级审批链）与 REQ-20260724-003（审批节点矩阵）核心部分，二者状态更新为"开发中"。

### REQ-20260727-001 作业票详情按PDF《危险作业审批表》还原固定签字栏
- **来源**：徐翎翔
- **提交人**：徐翎翔
- **录入日期**：2026-07-27
- **优先级**：P1
- **关联模块**：safeData.js（`workPermitSignForms`）、WorkTicket.vue（详情页"分级审批签字"区块）
- **状态**：已完成
- **原始需求**：
  - 用户质疑"作业票审批流程没有按我提供的文件修改"，核查发现审批链节点其实已与 `平台流程节点(1).xlsx` 一致，但 PDF《危险作业审批表》是"固定签字栏+按级别可空缺"样式（且比 xlsx 多"公司安全管理部门""技术工艺室""公司能源管理部门"等格），系统此前只做了"按级别动态裁剪链"，未还原 PDF 固定签字栏样式。用户确认方向：把 PDF 各作业类型审批表的完整签字栏还原进作业票详情页，并按「类型+级别」标记哪些需签、哪些可空缺。
- **AI 二次优化 / 落地记录**：
  - **数据层**：`safeData.js` 新增 `workPermitSignForms`——4 类作业（高处/临电/吊装/动火）各自 `columns`（依据制度附件 1-1~4-2 的完整签字栏，如高处含"公司安全管理部门意见"、吊装含"技术工艺室负责人意见"、动火含"公司能源管理部门（或武装保卫部）意见"），并配 `requiredByLevel`（按级别列出需签栏位）。
  - **前端**：`WorkTicket.vue` 详情页"分级审批链"区块替换为"🧾 XX审批表·分级审批签字"区块，列出全部签字格，按当前票「类型+级别」高亮"需签✓"（蓝框）、置灰标注"本级别不涉及·可空缺"，并附 PDF 原文备注语，保留级别/有效期信息行。
  - **校验**：8 张演示票级别均可映射到签字表；需签栏均为 columns 合法子集（0 问题）。生产构建 0 error / 53 warnings。
- **处理记录**：
  - 2026-07-27 还原 PDF 审批表签字栏；构建通过。桌面演示包按用户偏好未主动刷新。

---

## 模板（复制此段新增一条需求）

### REQ-YYYYMMDD-XXX 需求标题
- **来源**：业务人员 / 徐翎翔
- **提交人**：
- **录入日期**：YYYY-MM-DD
- **优先级**：P0 / P1 / P2
- **关联模块**：
- **状态**：待评估
- **原始需求**：
  - （粘贴业务人员/本人的原始描述，越具体越好）
- **AI 二次优化**：
  - **子任务拆解**：
  - **验收标准**：
  - **技术建议**：
  - **风险提示**：
- **处理记录**：
  - 
